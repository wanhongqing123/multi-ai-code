# 临时活动状态接入验证

2026-09-23。本次覆盖 Codex 源事件、Multi-AI Code 的腾讯 IM Web 桥接、Qt Desktop MaiChat 和 iOS MaiChat。交互约定见 [交互设计](交互设计.md) 第 10 节。

- 人工输入：三点气泡，无文字。Multi-AI Code 的手动 IM 输入也按人工输入发送。
- AICLI：来源于真实任务、思考、工具和审批事件，显示环形动画及简短状态。正文替换临时状态，后续执行阶段使用新活动 ID。
- 自定义在线消息在入库、未读与 AICLI 路由前分流；过期、乱序、已结束活动及账号切换有独立处理。
- macOS / Windows 的自绘 Markdown 正文显式使用随包 Inter、Noto Sans SC，代码使用 JetBrains Mono；字号和字重采用同一份主题，避免继承平台默认字体。

## 已完成验证

| 范围 | 结果 |
| --- | --- |
| TypeScript node / web | 类型检查通过 |
| IM 前端、编解码和转发相关测试 | 224 项通过；覆盖在线自定义消息、不进入正文路由、过期和乱序 |
| Desktop CTest | 46 / 46 通过；最后补充的临时气泡替换及字体布局回归也通过 |
| iOS MaiChatCoreTests | 206 / 206 通过 |
| iOS 完整 Workspace | Release 模拟器构建通过 |
| Electron | 生产构建通过 |
| Codex TUI | 4,141 通过（含 1 项重试通过），15 失败，6 跳过；新增活动状态测试通过 |
| Codex 修改前对照 | 上述 15 项在修改前版本也全部失败，涉及会话归档/名称查询、终端字形和既有快照；不记为全量通过 |
| Codex clippy / fmt | 完成，clippy 有既有警告 |

## 验证边界

以上是自动化测试和构建结果。尚未完成 Windows 与 macOS 实机逐像素对照，也未完成两台已安装客户端经公网互发状态的人工验收。操作系统字体栅格化可能仍有细小差别。

AICLI 状态发送需要同时更新 Multi-AI Code 的宿主代码和 Codex 内核；只更新 MaiChat 接收端不能使旧发送端自动产生新状态。当前 IM 正文按完整 message item 转发，并非逐 token 传输。
